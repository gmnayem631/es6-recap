const numbers = [1, 2, 3, 4, 5, 6];

// const squared = numbers.map((number) => {
//   return number * number;
// });

// console.log(squared);

numbers.forEach((number) => {
  console.log(number);
});
